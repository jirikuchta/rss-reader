version: "3"
services:
  app:
    build:
      context: .
      dockerfile: ./docker/tests/Dockerfile
    volumes:
      - ./rss_reader:/rss_reader
      - ./tests:/tests
    ports:
      - 5000:5000
    environment:
      SECRET_KEY: "secret"
      DATABASE_URI: "sqlite:///:memory:"
